#include <stdio.h>

int main() {
    int w;
    scanf("%d", &w);  // Đọc trọng lượng của quả dưa hấu
    
    // Kiểm tra nếu w là số chẵn và lớn hơn 2
    if (w % 2 == 0 && w > 2) {
        printf("YES\n");
    } else {
        printf("NO\n");
    }
    
    return 0;
}
